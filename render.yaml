services:
  - name: backend
    type: web
    runtime: python
    rootDir: login_backend
    buildCommand: pip install -r app/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
